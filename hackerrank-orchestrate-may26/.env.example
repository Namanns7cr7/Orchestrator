# Copy this file to .env (which is gitignored) and fill in your key.
# The agent runs fully offline WITHOUT this key (deterministic extractive mode);
# setting it enables Claude (temperature=0) to phrase the response text.

ANTHROPIC_API_KEY=

# Optional: override the model used for response synthesis.
# ORCHESTRATE_MODEL=claude-opus-4-8
